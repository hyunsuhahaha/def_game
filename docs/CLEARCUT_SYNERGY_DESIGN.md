# 벌목 런 태그 시너지·중간 전문화 규격

## 핵심 재미

후반의 목표는 **나무가 미친 속도로 다시 자라는 숲과, 완성된 빌드가 숲을 한꺼번에 쓸어버리는 힘이 충돌하는 것**이다. 난이도를 나무 체력으로 올리지 않는다. 일반 나무는 끝까지 잘 쓰러지고, 대신 재생 주기·한 번에 살아나는 수·재생 프리즘의 복구량이 증가한다.

`숲 압력 = min(2.65, 1 + (스테이지-1)×0.12 + max(0,레벨-8)×0.035 + max(0,스테이지 경과-150)/900)`

압력은 HUD에 `x1.0~x2.7`로 표시한다. 전역 재생 간격, 재생 파동의 복구 수, 재생 프리즘의 복구 수와 시전에 적용한다. 일반 나무 HP에는 적용하지 않는다.

## 태그 집계 규칙

- 스킬을 처음 얻을 때 그 스킬의 두 태그가 각각 1씩 증가한다.
- 같은 스킬을 2~6레벨로 올려도 태그 수는 추가되지 않는다.
- 2개 효과는 런 레벨 6부터, 강한 4개 효과는 런 레벨 28부터 실제 활성화된다. 태그만 넓게 주워 초반에 완성 빌드 화력을 얻지 못하게 하는 시간 잠금이다.
- 카드에는 두 태그와 `현재 수 → 획득 후 수`를 표시한다.
- 카드의 태그에 마우스를 올리면 활성 구간과 정확한 효과를 표시한다.
- 화면 왼쪽은 현재 보유한 태그 엠블럼 아이콘만 세로로 표시한다. 이름·보유 수·단계 숫자·진행 막대·행 배경은 상시 표시하지 않으며, 아이콘에 마우스를 올렸을 때만 모든 구간을 확인한다.
- 이미 활성화한 단계는 태그 고유색, 아직 닿지 않은 단계는 낮은 명도로 구분한다.

## 아이콘 UI 규격

- 왼쪽 HUD는 64px 원본 픽셀 엠블럼을 넣은 팔각 소켓만 사용한다. 화면 위에는 아이콘 외의 시너지 텍스트를 두지 않는다.
- 7종 실루엣은 기동=날개 화살, 장판=동심 영역, 점화=불꽃, 생장=새싹, 강타=갈라진 충격, 야생=눈/발톱, 수확=통나무/곡물이다. 색만 바꾼 복제 아이콘을 쓰지 않는다.
- 레벨업 카드의 태그도 같은 엠블럼 배지를 사용하며 `현재→획득 후` 수치를 함께 표시한다.
- 호버 상세창은 반복 격자나 타일을 깔지 않은 고대비 불투명 바탕 위에 큰 엠블럼, 단계별 문장, 잠김·레벨 대기·활성 상태, 실제 기여 스킬을 표시한다. 글자 뒤에는 무늬가 없어야 한다.
- 태그 정체성은 단순 테두리나 패널 무늬가 아니라 전용 엠블럼 자체로 표현한다. 최종 아틀라스는 `assets/ui/synergy-emblems-pixel-v1.png`, 크롬은 아이콘 소켓에만 쓰는 `assets/ui/synergy-chrome-pixel-v1.png`다. 제작 방향 원본은 `assets/ui/concepts/synergy-emblems-concept-v1.png`에 보관한다.
- 오프스크린 실제 경로 캡처는 `docs/previews/clearcut-synergy-ui-v3.png`에서 검수한다.

## 시너지 효과

| 태그 | 2개 · Lv.6부터 | 4개 · Lv.28부터 |
|---|---|---|
| 야생 | 박쥐·까마귀 공격 주기 -22%, 피해 +18% | 없음 |
| 생장 | 생장 스킬 범위 +14%, 12그루마다 발아 파동 | 파동이 7그루마다 더 넓고 강하게 발생 |
| 기동 | 공용 자동 스킬 주기 -10%, 투사체 속도 +10% | 공용 자동 스킬 주기 -25%, 투사체 속도 +25% |
| 장판 | 장판 태그 공용 스킬 범위 +12% | 범위 +28%, 지속 피해 주기 -28% |
| 강타 | 공용 자동 스킬 피해 +12%, 12그루마다 벌목 충격파 | 공용 자동 스킬 피해 +28%, 충격파가 7그루마다 강화 |
| 점화 | 불씨 전이 확률 +18%, 전이 범위 +14% | 불씨 전이 확률 +38%, 전이 범위 +32% |
| 수확 | 목재 경험치 +12% | 목재 경험치 +28% |

## 현재 인게임 스킬 태그

| 계열 | 스킬 → 태그 |
|---|---|
| 생계형 나무꾼 | 야근 수당 `강타/기동`, 이번 달 목표 초과 `기동/강타`, 산재 위험수당 `강타/장판` |
| 흡연자 | 꽁초 투척 `점화/기동`, 건조주의보 무시 `점화/장판`, 라이터 기름 유출 `점화/강타`, 마른 건초더미 `점화/장판`, 도넛 강화 `장판/기동` |
| 비건 단체 회장 | 대왕 포크 `수확/강타`, 뷔페용 포크 `수확/기동`, 접시 비우기 `수확/장판`, 한 그릇 더 `수확/기동` |
| 공용 억제 | 강제 성장 `생장/수확` |
| 부동산 개발업자 | 말뚝 박기 `강타/기동`, 중장비 투입 `강타/기동`, 철거 폭파 `강타/점화`, 부지 정지 `장판/강타` |
| 코인 채굴꾼 | 복리 발톱 `강타/기동`, 지하 강제집행 `강타/장판`, 브루트포스 `기동/강타` |
| 철학자 | 끝없는 설교 `장판/기동`, 부흥회 `장판/생장`, 각주 남발 `기동/장판`, 목청 키우기 `장판/강타`, 침샘 발달 `생장/장판` |
| 공용 자동 | 박쥐 떼 `야생/기동`, 가시 오라 `생장/장판`, 까마귀 습격 `야생/기동`, 덩굴 채찍 `생장/강타`, 부메랑 도끼 `기동/강타`, 씨앗 지뢰 `생장/장판`, 번개 사슬 `기동/강타` |

포자 구름은 현재 인게임 스킬이 아니며 이 시스템에 넣지 않는다.

## 3레벨 전문화

씨앗 지뢰와 부메랑 도끼가 2레벨에서 3레벨이 되는 순간 일반 3택을 잠시 중단하고 전용 전문화 3택을 연다. 한 런에서 하나만 고르며 되돌릴 수 없다.

### 씨앗 지뢰

- **대형 지뢰:** 폭발 반경 +42%, 피해 +35%.
- **산탄 지뢰:** 본체 폭발 뒤 작은 씨앗 6개가 흩어져 짧은 지연 후 재폭발.
- **발아 지뢰:** 폭발 자리에 4초간 덩굴밭이 남아 반복 타격.

### 부메랑 도끼

- **넓은 도끼:** 회전 피격 폭 +45%, 피해 +18%.
- **빠른 왕복:** 비행 속도 +55%, 발사 주기 -35%.
- **튕기는 도끼:** 나가는 도중 몬스터를 맞히면 최대 3회 다른 몬스터로 재조준.

## 흡연자 6레벨 무기 진화

`꽁초 투척`이 6레벨이 되면 일반 카드 선택을 잠시 중단하고 전용 2택을 연다. 선택 뒤 마우스 기본 공격만 새 무기로 교체되며, 기존 꽁초 투척은 2.6초 주기의 자동 패시브로 계속 작동해 바닥 불씨 전이 구조를 유지한다.

- **전자담배:** 짧은 주기의 청록·보라 증기탄을 연사한다. 보이는 증기 덩어리와 같은 넉넉한 swept-circle 판정으로 나무와 몬스터를 관통한다.
- **폭죽 발사기:** 목표 지점으로 로켓을 곡사 발사한다. 착탄 시 빨강·금색·청록·초록·자홍 불꽃이 6프레임으로 퍼지며 넓은 범위에 피해를 주고 식물형 대상에는 불을 붙인다.
- 장비는 `assets/characters/ingame/smoker-weapon-evolution-equipment-v1.png`, 증기/로켓은 `assets/effects/smoker-weapon-evolution-fx-v1.png`, 폭죽 폭발은 384px 셀 30프레임의 `assets/effects/smoker-firework-burst-v2.png`를 nearest 필터로 사용한다.
- 제작 시안은 `assets/effects/concepts/smoker-weapon-evolutions-concept-v1.png`, 실제 크기 오프스크린 검수본은 `docs/previews/smoker-weapon-evolutions-v1.png`다.

## 밸런스 가드레일

- 후반 성장감은 적·나무 HP 증가보다 동시 타격 수, 파동, 투사체 수, 재생 물량으로 만든다.
- 재생이 강해져도 완성 빌드는 한 구역을 한 번에 비울 수 있어야 한다.
- 시너지 효과 설명은 실제 코드에 연결된 수치만 적는다.
- 새 스킬을 추가할 때만 태그를 추가한다. 제거된 아이디어를 태그 채우기용으로 되살리지 않는다.
- 스킬 태그·전문화·융합을 바꾸면 `docs/character_dossier.html`도 같은 커밋에서 갱신한다.

## 30초 헤드리스 플레이 기준

생산 전투 로직으로 HP 6 나무 651그루 사이의 여섯 지점을 5초씩 이동하며 기본 공격을 누른 상태로 측정한다. 몬스터 8마리는 자동 스킬이 실제 표적 선택을 하도록 플레이어 주변을 이동한다.

| 구간 | 빌드 | 30초 벌목 | 초당 벌목 |
|---|---|---:|---:|
| 시작 | Lv.3, 야근 수당 1 + 부메랑 1 | 12 | 0.40 |
| 초반 | Lv.8, 저레벨 스킬 6종 분산 | 71 | 2.37 |
| 중반 | Lv.19, 3레벨 스킬 6종 + 전문화 | 365 | 12.17 |
| 후반 | Lv.43, 6레벨 완성 빌드 | 651 | 21.70 |
| 후반 재생전 | 위 빌드 + 재생 프리즘 4기 | 벌목 1083 / 재생 528 | 벌목 36.10 |

재생전 수치는 같은 나무를 다시 베는 횟수를 포함한다. 완성 빌드가 빠르게 베지만 프리즘도 30초 동안 528그루를 복구하므로, 화면에서는 `우두두 재생 ↔ 우두두 벌목`이 동시에 지속된다.
